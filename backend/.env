# Porta onde a API irá rodar
PORT=4000

# Chave usada pelo middleware X-API-Key (alterar em produção)
API_KEY=super-secret-api-key

# Caminho para o JSON da Service Account do Firebase Admin SDK
GOOGLE_APPLICATION_CREDENTIALS=./credentials/serviceAccount.json

# ID de usuário padrão usado quando o cabeçalho X-User-Id não é enviado
DEFAULT_USER_ID=default-user
